# CTF Python toolkit, installed into the "prophesy" venv.
#
# Intentionally unpinned: pip resolves current wheels for the image's Python
# (Debian bookworm = 3.11). The previous hard pins (angr==9.2.68, etc.) are old
# enough that pip builds them from sdist, and their legacy setup.py imports
# pkg_resources, which modern setuptools (>=81) removed -- so the build fails.
# Re-pin from a known-good build if you want byte-for-byte reproducibility.
angr
pwntools
ropper
unicorn
Pillow
xortool
